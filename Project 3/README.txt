The program is run from Driver.java, so if running in an IDE, run the main method from Driver.java
If running in terminal, you may need to compile all 3 classes, so make sure to do that before running Driver.java


Once you run the program, text-based user interface will appear, and you can interact with it and play the game.